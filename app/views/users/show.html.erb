<h1>Your Employee Info</h1>
<table class="table">
  <thead>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Title</th>
      <th>Manager/Employee</th>
    </tr>
  </thead>
  <tr>
    <td><%= link_to @user.first_name, company_user_path(current_company.id,current_user.id) %></td>
    <td><%= link_to @user.last_name, company_user_path(current_company.id,current_user.id) %></td>
    <td><%= link_to @user.title, company_user_path(current_company.id,current_user.id) %></td>
    <td><%= link_to user.access, company_user_path(current_company.id,current_user.id) %></td>
    <td><%= link_to "edit", edit_company_user_path(current_company.id,current_user.id) %></td>
  </tr>
  </table>
<hr>
